{
  "name": "timeline",
  "version": "0.1.0",
  "scripts": {
    "start": "browser-sync start --files='index.html, css/*.css, js/*.js, js/**/*.js' --server"
  },
  "devDependencies": {
    "browser-sync": "^2.7.6"
  },
  "license": "MIT",
  "private": true
}
