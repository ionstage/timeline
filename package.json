{
  "name": "timeline",
  "version": "0.1.0",
  "scripts": {
    "browser-sync": "browser-sync start --files='index.html, css/*.css, js/*.js, js/**/*.js' --server"
  },
  "devDependencies": {
    "browser-sync": "*"
  },
  "license": "MIT",
  "private": true
}
